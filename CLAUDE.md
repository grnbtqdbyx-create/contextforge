# Claude Notes

ContextForge is a local-first TypeScript CLI for Codex and Claude Code context
audits.

Prefer small, test-backed changes. For feature work, add or update a fixture in
`fixtures/` and a Vitest test in `tests/` before changing runtime modules.

Use `docs/research/adjacent-tools.md` when updating product positioning or README
copy.
